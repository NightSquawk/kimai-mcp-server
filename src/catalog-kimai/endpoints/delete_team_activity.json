{
  "operationId": "delete_team_activity",
  "resource": "teams",
  "category": "Team",
  "name": "Revoke activity access",
  "method": "DELETE",
  "path": "/api/teams/{id}/activities/{activityId}",
  "description": "This removes access to the activity from the team.",
  "writeOperation": true,
  "destructive": true,
  "paramLocation": "query",
  "pathParams": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "in": "path",
      "description": "The team whose permission will be revoked",
      "pattern": "\\d+"
    },
    {
      "name": "activityId",
      "type": "string",
      "required": true,
      "in": "path",
      "description": "The activity to remove (Activity ID)",
      "pattern": "\\d+"
    }
  ],
  "params": [],
  "bodySchema": null,
  "bodyContentType": null,
  "responses": {
    "200": {
      "description": "Returns the team without the activity"
    }
  },
  "requestSample": "curl -X DELETE '$KIMAI_BASE_URL/api/teams/{id}/activities/{activityId}' -H 'Authorization: Bearer $KIMAI_API_TOKEN'"
}
