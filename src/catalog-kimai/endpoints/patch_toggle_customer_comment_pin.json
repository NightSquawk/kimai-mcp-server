{
  "operationId": "patch_toggle_customer_comment_pin",
  "resource": "customers",
  "category": "Customer",
  "name": "Pin customer comment",
  "method": "PATCH",
  "path": "/api/customers/{id}/comments/{comment}/pin",
  "description": "This toggles the `pinned` status of the given comment.",
  "writeOperation": true,
  "destructive": false,
  "paramLocation": "query",
  "pathParams": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "in": "path",
      "description": "The customer whose comment will be pinned or unpinned",
      "pattern": "\\d+"
    },
    {
      "name": "comment",
      "type": "string",
      "required": true,
      "in": "path",
      "description": "The comment whose pinned status will be toggled",
      "pattern": "\\d+"
    }
  ],
  "params": [],
  "bodySchema": null,
  "bodyContentType": null,
  "responses": {
    "200": {
      "description": "Returns the updated customer comment"
    }
  },
  "requestSample": "curl -X PATCH '$KIMAI_BASE_URL/api/customers/{id}/comments/{comment}/pin' -H 'Authorization: Bearer $KIMAI_API_TOKEN'"
}
