{
  "operationId": "patch_duplicate_timesheet",
  "resource": "timesheets",
  "category": "Timesheet",
  "name": "Duplicate timesheet",
  "method": "PATCH",
  "path": "/api/timesheets/{id}/duplicate",
  "description": "Duplicate timesheet",
  "writeOperation": true,
  "destructive": false,
  "paramLocation": "query",
  "pathParams": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "in": "path",
      "description": "Timesheet ID to duplicate",
      "pattern": "\\d+"
    }
  ],
  "params": [],
  "bodySchema": null,
  "bodyContentType": null,
  "responses": {
    "200": {
      "description": "Duplicates a timesheet, resetting the export state only."
    }
  },
  "requestSample": "curl -X PATCH '$KIMAI_BASE_URL/api/timesheets/{id}/duplicate' -H 'Authorization: Bearer $KIMAI_API_TOKEN'"
}
