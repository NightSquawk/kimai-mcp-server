{
  "operationId": "delete_team_customer",
  "resource": "teams",
  "category": "Team",
  "name": "Revoke customer access",
  "method": "DELETE",
  "path": "/api/teams/{id}/customers/{customerId}",
  "description": "This removes access to the customer from the team.",
  "writeOperation": true,
  "destructive": true,
  "paramLocation": "query",
  "pathParams": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "in": "path",
      "description": "The team whose permission will be revoked",
      "pattern": "\\d+"
    },
    {
      "name": "customerId",
      "type": "string",
      "required": true,
      "in": "path",
      "description": "The customer to remove (Customer ID)",
      "pattern": "\\d+"
    }
  ],
  "params": [],
  "bodySchema": null,
  "bodyContentType": null,
  "responses": {
    "200": {
      "description": "Returns the team without the customer"
    }
  },
  "requestSample": "curl -X DELETE '$KIMAI_BASE_URL/api/teams/{id}/customers/{customerId}' -H 'Authorization: Bearer $KIMAI_API_TOKEN'"
}
