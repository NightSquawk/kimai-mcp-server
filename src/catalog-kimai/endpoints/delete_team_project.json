{
  "operationId": "delete_team_project",
  "resource": "teams",
  "category": "Team",
  "name": "Revoke project access",
  "method": "DELETE",
  "path": "/api/teams/{id}/projects/{projectId}",
  "description": "This removes access to the project from the team.",
  "writeOperation": true,
  "destructive": true,
  "paramLocation": "query",
  "pathParams": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "in": "path",
      "description": "The team whose permission will be revoked",
      "pattern": "\\d+"
    },
    {
      "name": "projectId",
      "type": "string",
      "required": true,
      "in": "path",
      "description": "The project to remove (Project ID)",
      "pattern": "\\d+"
    }
  ],
  "params": [],
  "bodySchema": null,
  "bodyContentType": null,
  "responses": {
    "200": {
      "description": "Returns the team without the project"
    }
  },
  "requestSample": "curl -X DELETE '$KIMAI_BASE_URL/api/teams/{id}/projects/{projectId}' -H 'Authorization: Bearer $KIMAI_API_TOKEN'"
}
