{
  "operationId": "post_customer_comment",
  "resource": "customers",
  "category": "Customer",
  "name": "Add comment for customer",
  "method": "POST",
  "path": "/api/customers/{id}/comments",
  "description": "Add comment for customer",
  "writeOperation": true,
  "destructive": false,
  "paramLocation": "body",
  "pathParams": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "in": "path",
      "description": "The customer to add the comment for",
      "pattern": "\\d+"
    }
  ],
  "params": [],
  "bodySchema": {
    "type": "object",
    "properties": {
      "message": {
        "type": "string",
        "description": "The actual comment (markdown is supported)"
      },
      "pinned": {
        "type": "boolean",
        "description": "Pinned comments always appear first"
      }
    },
    "required": [
      "message"
    ]
  },
  "bodyContentType": "application/json",
  "responses": {
    "200": {
      "description": "Returns the newly created customer comment"
    }
  },
  "requestSample": "curl -X POST '$KIMAI_BASE_URL/api/customers/{id}/comments' -H 'Authorization: Bearer $KIMAI_API_TOKEN' -H 'Content-Type: application/json' -d '{...}'"
}
