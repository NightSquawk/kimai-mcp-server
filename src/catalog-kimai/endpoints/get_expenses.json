{
  "operationId": "get_expenses",
  "resource": "expenses",
  "category": "Expense",
  "name": "List expenses",
  "method": "GET",
  "path": "/api/expenses",
  "description": "Returns a collection of expenses. Requires the Kimai Expenses plugin. HAND-AUTHORED: this endpoint is absent from the open-source /api/doc the catalog was generated from, so its parameters were reconstructed from Kimai's shared collection-filter conventions and from live calls made by kimai_list_expenses in v0.2.0. Treat the parameter list as observed rather than specified, and expect a 404 on any instance without the plugin.",
  "writeOperation": false,
  "destructive": false,
  "paramLocation": "query",
  "pathParams": [],
  "params": [
    {
      "name": "page",
      "type": "string",
      "required": false,
      "in": "query",
      "description": "Page number, 1-based.",
      "default": "1"
    },
    {
      "name": "size",
      "type": "string",
      "required": false,
      "in": "query",
      "description": "Entries per page.",
      "default": "50"
    }
  ],
  "bodySchema": null,
  "bodyContentType": null,
  "responses": {
    "200": {
      "description": "Returns a collection of expense entities"
    },
    "404": {
      "description": "Expenses plugin is not installed on this instance"
    }
  },
  "requestSample": "curl -X GET '$KIMAI_BASE_URL/api/expenses' -H 'Authorization: Bearer $KIMAI_API_TOKEN'",
  "pluginOnly": true
}
