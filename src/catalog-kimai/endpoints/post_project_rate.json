{
  "operationId": "post_project_rate",
  "resource": "projects",
  "category": "Project",
  "name": "Add rate for project",
  "method": "POST",
  "path": "/api/projects/{id}/rates",
  "description": "Add rate for project",
  "writeOperation": true,
  "destructive": false,
  "paramLocation": "body",
  "pathParams": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "in": "path",
      "description": "The project to add the rate for",
      "pattern": "\\d+"
    }
  ],
  "params": [],
  "bodySchema": {
    "type": "object",
    "properties": {
      "internalRate": {
        "type": "number",
        "description": "The internal rate (eg. 10.0 or 10)"
      },
      "isFixed": {
        "type": "boolean"
      },
      "rate": {
        "type": "number",
        "description": "The rate (eg. 10.5)"
      },
      "user": {
        "type": "integer",
        "description": "User ID"
      }
    },
    "required": [
      "rate"
    ]
  },
  "bodyContentType": "application/json",
  "responses": {
    "200": {
      "description": "Returns the new created rate"
    }
  },
  "requestSample": "curl -X POST '$KIMAI_BASE_URL/api/projects/{id}/rates' -H 'Authorization: Bearer $KIMAI_API_TOKEN' -H 'Content-Type: application/json' -d '{...}'"
}
