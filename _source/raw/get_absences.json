{
  "method": "GET",
  "path": "/api/absences",
  "operationId": "get_absences",
  "summary": "List absences",
  "description": "Returns absences (holiday, sickness, time off) for a user over a date range. Requires the Kimai Work Contract plugin. Useful for explaining gaps in a timesheet report before assuming time went unlogged. This endpoint is NOT paginated: it returns the full range in one response, so a wide begin/end can return a large payload. HAND-AUTHORED: see get_expenses for what that means about parameter fidelity.",
  "tags": [
    "Absence"
  ],
  "deprecated": false,
  "pathParams": [],
  "params": [
    {
      "name": "user",
      "type": "string",
      "required": false,
      "in": "query",
      "description": "User ID to fetch absences for. Defaults to the API token owner."
    },
    {
      "name": "begin",
      "type": "string",
      "required": false,
      "in": "query",
      "description": "Range start as an HTML5 local date-time, e.g. 2026-08-01T00:00:00.",
      "format": "date-time"
    },
    {
      "name": "end",
      "type": "string",
      "required": false,
      "in": "query",
      "description": "Range end as an HTML5 local date-time.",
      "format": "date-time"
    }
  ],
  "bodySchema": null,
  "bodyContentType": null,
  "responses": {
    "200": {
      "description": "Returns a collection of absence entities"
    },
    "404": {
      "description": "Work Contract plugin is not installed on this instance"
    }
  },
  "pluginOnly": true
}
